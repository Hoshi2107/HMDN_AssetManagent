@{
    ViewBag.Title = "Vòng đời & Khấu hao";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
  :root {
    --accent: #2563eb;

    --accent-light: #3b82f6;
    --danger: #ef4444;
    --warning: #f59e0b;
    --success: #10b981;
    --muted: #94a3b8;
    --card-bg: #ffffff;
    --body-bg: #f1f5f9;
    --border: #e2e8f0;
    --text: #1e293b;
    --text-muted: #64748b;
    --disposed: #64748b;
  }
  
  .page-title { font-size: 22px; font-weight: 700; color: var(--text); }
  .page-sub { font-size: 13px; color: var(--text-muted); margin-top: 3px; }

  /* STATS */
  .stats-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 16px; margin-bottom: 24px; }
  .stat-card { background: white; border-radius: 12px; padding: 18px 20px; border: 1px solid var(--border); display: flex; justify-content: space-between; transition: box-shadow .18s; }
  .stat-card:hover { box-shadow: 0 4px 20px rgba(0,0,0,0.07); }
  .stat-info .label { font-size: 11px; font-weight: 600; color: var(--muted); text-transform: uppercase; }
  .stat-info .value { font-size: 28px; font-weight: 700; color: var(--text); margin: 4px 0 2px; }
  .stat-info .delta { font-size: 11.5px; color: var(--success); font-weight: 500; }
  .stat-icon { width: 42px; height: 42px; border-radius: 10px; display: flex; align-items: center; justify-content: center; font-size: 18px; }
  .si-blue { background: #eff6ff; color: var(--accent); }
  .si-green { background: #f0fdf4; color: var(--success); }
  .si-yellow { background: #fffbeb; color: var(--warning); }
  .si-gray { background: #f1f5f9; color: var(--disposed); }

  /* FILTERS */
  .filter-bar { background: white; border-radius: 12px; border: 1px solid var(--border); padding: 16px 20px; display: flex; align-items: center; gap: 12px; margin-bottom: 16px; flex-wrap: wrap; }
  .filter-group { display: flex; flex-direction: column; gap: 3px; }
  .filter-group label { font-size: 11px; color: var(--muted); font-weight: 500; }
  .filter-select { background: #f8fafc; border: 1px solid var(--border); border-radius: 7px; padding: 7px 12px; font-size: 12.5px; outline: none; min-width: 150px; }
  .btn-filter { background: white; border: 1px solid var(--border); border-radius: 7px; padding: 7px 14px; font-size: 12.5px; cursor: pointer; display: flex; align-items: center; gap: 6px; font-weight: 500; margin-top: 16px; transition: background .15s; }
  .btn-filter:hover { background: #f1f5f9; }

  /* TABLE */
  .table-card { background: white; border-radius: 12px; border: 1px solid var(--border); overflow: hidden; }
  .table-top { padding: 16px 20px; border-bottom: 1px solid var(--border); display: flex; justify-content: space-between; align-items: center; }
  .table-title { font-size: 14px; font-weight: 600; }
  .table-responsive { overflow-x: auto; width: 100%; }
  .top-scrollbar { overflow-x: auto; width: 100%; }
  .top-scrollbar::-webkit-scrollbar { height: 10px; }
  .top-scrollbar::-webkit-scrollbar-track { background: #f1f5f9; }
  .top-scrollbar::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 5px; }
  .top-scrollbar::-webkit-scrollbar-thumb:hover { background: #94a3b8; }
  table { width: 100%; border-collapse: collapse; min-width: 1200px; }
  thead th { background: #f8fafc; padding: 11px 16px; font-size: 11.5px; font-weight: 600; color: var(--muted); text-transform: uppercase; text-align: left; border-bottom: 1px solid var(--border); }
  tbody td { padding: 13px 16px; font-size: 13px; border-bottom: 1px solid #f1f5f9; vertical-align: middle; }
  tbody tr:hover { background: #f8fafc; }
  .device-id { color: var(--accent); font-weight: 600; font-size: 12px; }
  .device-name { font-weight: 600; color: var(--text); }
  .money { font-family: monospace; font-size: 12.5px; font-weight: 500; text-align: right; }
  .th-money { text-align: right !important; }

  /* PAGINATION & SORTING */
  .sortable { cursor: pointer; user-select: none; transition: color .15s; }
  .sortable:hover { color: var(--accent); }
  .sort-icon { font-size: 10px; margin-left: 4px; color: #cbd5e1; }
  .sort-icon.active { color: var(--accent); }
  
  .pagination { display: flex; justify-content: space-between; align-items: center; padding: 12px 20px; border-top: 1px solid var(--border); background: white; flex-wrap: wrap; gap: 12px; }
  .page-info { font-size: 12.5px; color: var(--muted); }
  .page-controls { display: flex; gap: 6px; }
  .page-btn { background: white; border: 1px solid var(--border); border-radius: 6px; padding: 5px 10px; font-size: 12.5px; cursor: pointer; color: var(--text); min-width: 32px; text-align: center; }
  .page-btn:hover:not(:disabled) { background: #f8fafc; color: var(--accent); }
  .page-btn:disabled { opacity: 0.5; cursor: not-allowed; }
  .page-btn.active { background: var(--accent); color: white; border-color: var(--accent); }

  /* BADGES */
  .status-badge { display: inline-flex; align-items: center; gap: 5px; font-size: 11.5px; font-weight: 600; padding: 4px 10px; border-radius: 20px; }
  .status-badge::before { content:''; width:6px; height:6px; border-radius:50%; background: currentColor; }
  .s-active { background: #f0fdf4; color: #16a34a; }
  .s-suspended { background: #fffbeb; color: #d97706; }
  .s-disposed { background: #f1f5f9; color: var(--disposed); }
  .s-replaced { background: #eff6ff; color: var(--accent); }
  .replaced-link { color: var(--accent); text-decoration: underline; font-weight: 600; font-size: 11.5px; cursor: pointer; }

  /* ACTIONS */
  .action-btn { background: none; border: none; cursor: pointer; color: var(--muted); font-size: 15px; padding: 4px 8px; border-radius: 5px; }
  .action-btn:hover { background: #e2e8f0; color: var(--text); }
  .dropdown { position: relative; display: inline-block; }
  .dropdown-menu { display: none; position: absolute; right: 0; top: 100%; background: white; min-width: 180px; box-shadow: 0 10px 25px rgba(0,0,0,0.1); border-radius: 8px; border: 1px solid var(--border); z-index: 10; overflow: hidden; }
  .dropdown.show .dropdown-menu { display: block; animation: fadeIn .15s ease; }
  .dropdown-item { padding: 10px 14px; font-size: 12.5px; color: var(--text); display: flex; align-items: center; gap: 8px; cursor: pointer; transition: background .15s; }
  .dropdown-item:hover { background: #f8fafc; color: var(--accent); }
  .dropdown-item i { width: 14px; text-align: center; color: var(--muted); }
  .dropdown-item:hover i { color: var(--accent); }

  /* MODALS */
  .custom-modal-overlay { position: fixed; inset: 0; background: rgba(15, 23, 42, 0.6); z-index: 1050; display: flex; align-items: center; justify-content: center; backdrop-filter: blur(4px); animation: fadeIn .2s ease; }
  .custom-modal { background: white; border-radius: 16px; width: 500px; max-width: 90vw; box-shadow: 0 25px 50px -12px rgba(0,0,0,0.25); animation: popUp .25s ease; }
  .custom-modal.large { width: 750px; }
  @@keyframes fadeIn { from { opacity:0; } to { opacity:1; } }
  @@keyframes popUp { from { transform: scale(.95); opacity:0; } to { transform: scale(1); opacity:1; } }
  .modal-header { padding: 18px 24px; border-bottom: 1px solid var(--border); display: flex; align-items: center; justify-content: space-between; }
  .modal-title { font-size: 16px; font-weight: 700; }
  .btn-close-modal { background: none; border: none; color: var(--muted); font-size: 20px; cursor: pointer; }
  .modal-body { padding: 24px; max-height: 65vh; overflow-y: auto; }
  .modal-footer { padding: 16px 24px; border-top: 1px solid var(--border); background: #f8fafc; display: flex; justify-content: flex-end; gap: 12px; border-bottom-left-radius: 16px; border-bottom-right-radius: 16px; }

  .form-label { font-size: 13px; font-weight: 600; color: var(--text); margin-bottom: 6px; display: block; }
  .form-control { width: 100%; border: 1px solid var(--border); border-radius: 8px; padding: 10px 14px; font-size: 13.5px; outline: none; transition: all .2s; }
  .form-control:focus { border-color: var(--accent); box-shadow: 0 0 0 3px rgba(37,99,235,0.1); }
  .btn-primary { background: var(--accent); color: white; border: none; border-radius: 8px; padding: 9px 18px; font-size: 13.5px; font-weight: 600; cursor: pointer; transition: all .2s; }
  .btn-primary:hover:not(:disabled) { background: var(--accent-light); }
  .btn-secondary { background: white; color: var(--text); border: 1px solid var(--border); border-radius: 8px; padding: 9px 18px; font-size: 13.5px; font-weight: 500; cursor: pointer; transition: all .2s; }
  .btn-secondary:hover:not(:disabled) { background: #f8fafc; }
  .btn-sm { padding: 7px 12px; font-size: 12px; }

  /* CHARTS & BATCH ACTIONS */
  .charts-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; margin-bottom: 24px; }
  .chart-card { background: white; border-radius: 12px; border: 1px solid var(--border); padding: 20px; }
  .chart-title { font-size: 14.5px; font-weight: 600; color: var(--text); margin-bottom: 16px; display: flex; justify-content: space-between; align-items: center; }
  .chart-container { position: relative; height: 240px; width: 100%; display: flex; justify-content: center; }

  .batch-bar { background: #1e293b; color: white; border-radius: 10px; padding: 12px 20px; display: flex; justify-content: space-between; align-items: center; margin-bottom: 16px; animation: slideDown 0.3s ease; box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1); }
  .batch-count { font-size: 13.5px; font-weight: 500; }
  .batch-actions { display: flex; gap: 10px; }
  @@keyframes slideDown { from { transform: translateY(-15px); opacity: 0; } to { transform: translateY(0); opacity: 1; } }
  
  .checkbox-cell { width: 40px; text-align: center; }
  .custom-checkbox { width: 16px; height: 16px; cursor: pointer; accent-color: var(--accent); }

  /* DEPRECIATION LOGS */
  .log-timeline { display: flex; flex-direction: column; gap: 12px; }
  .log-item { background: #f8fafc; border: 1px solid var(--border); border-radius: 10px; padding: 16px; display: flex; flex-direction: column; gap: 8px; }
  .log-header { display: flex; justify-content: space-between; align-items: center; font-size: 14px; font-weight: 600; color: var(--text); border-bottom: 1px dashed var(--border); padding-bottom: 8px; }
  .log-year { color: var(--accent); }
  .log-calc { font-size: 11.5px; color: var(--muted); font-weight: 400; }
  .log-stats { display: flex; justify-content: space-between; gap: 20px; }
  .l-stat { flex: 1; display: flex; flex-direction: column; }
  .l-label { font-size: 11px; color: var(--muted); margin-bottom: 4px; text-transform: uppercase; }
  .l-val { font-size: 14px; font-weight: 600; font-family: monospace; }
  .val-minus { color: var(--danger); }
  .log-note { font-size: 12px; color: var(--text-muted); background: white; padding: 8px 12px; border-radius: 6px; border: 1px solid var(--border); margin-top: 4px; }
</style>

<div id="app">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px; flex-wrap: wrap; gap: 16px;">
        <div>
            <div class="page-title">Vòng đời & Khấu hao Thiết bị</div>
            <div class="page-sub">Theo dõi tình trạng (LifeStatus), tính toán khấu hao tự động và quản lý lịch sử thay thế.</div>
        </div>
        <div style="display: flex; gap: 12px; align-items: center; flex-wrap: wrap;">
            <div style="position:relative; width: 260px;">
                <i class="fa-solid fa-magnifying-glass" style="position:absolute; left:12px; top:11px; color:var(--muted)"></i>
                <input type="text" placeholder="Tìm kiếm thiết bị khấu hao..." v-model="searchQuery" style="width:100%; padding:9px 16px 9px 36px; border:1px solid var(--border); border-radius:8px; outline:none; font-size:13px; background:white;">
            </div>
            <button class="btn-primary" v-if="hasPermission('VongDoiKhauHao', 'CREATE')" @@click="openAddAssetModal" style="display: inline-flex; align-items: center; gap: 8px; font-weight: 600; padding: 10px 16px; border-radius: 8px; border: none; background: var(--accent); color: white; cursor: pointer; transition: background .15s; height: 38px;">
                <i class="fa-solid fa-plus"></i> Thêm tài sản
            </button>
        </div>
    </div>

    <!-- STATS -->
    <div class="stats-grid">
        <div class="stat-card">
            <div class="stat-info">
                <div class="label">Tổng thiết bị</div>
                <div class="value">{{ devices.length }}</div>
                <div class="delta"><i class="fa-solid fa-arrow-up"></i> Tăng 12%</div>
            </div>
            <div class="stat-icon si-blue"><i class="fa-solid fa-layer-group"></i></div>
        </div>
        <div class="stat-card">
            <div class="stat-info">
                <div class="label">Đang khấu hao (Active)</div>
                <div class="value">{{ countStatus('active') }}</div>
                <div class="delta" style="color:var(--text-muted)">Sinh lời ổn định</div>
            </div>
            <div class="stat-icon si-green"><i class="fa-solid fa-heart-pulse"></i></div>
        </div>
        <div class="stat-card">
            <div class="stat-info">
                <div class="label">Tạm ngưng do hỏng</div>
                <div class="value">{{ countStatus('suspended') }}</div>
                <div class="delta" style="color:var(--warning)">Cần đánh giá sửa chữa</div>
            </div>
            <div class="stat-icon si-yellow"><i class="fa-solid fa-wrench"></i></div>
        </div>
        <div class="stat-card">
            <div class="stat-info">
                <div class="label">Đã thanh lý / Thay thế</div>
                <div class="value">{{ countStatus('disposed') + countStatus('replaced') }}</div>
                <div class="delta" style="color:var(--disposed)">Kết thúc vòng đời</div>
            </div>
            <div class="stat-icon si-gray"><i class="fa-solid fa-box-archive"></i></div>
        </div>
        <div class="stat-card" style="border-color:var(--accent-light);">
            <div class="stat-info">
                <div class="label">Tổng giá trị còn lại</div>
                <div class="value" style="font-size: 22px; color:var(--accent);">{{ formatMoney(totalClosingValue) }} đ</div>
                <div class="delta" style="color:var(--accent)">Sau khấu hao</div>
            </div>
            <div class="stat-icon si-blue"><i class="fa-solid fa-coins"></i></div>
        </div>
    </div>

    <!-- CHARTS -->
    <div class="charts-grid" v-show="devices.length > 0">
        <div class="chart-card">
            <div class="chart-title">
                <span><i class="fa-solid fa-chart-pie" style="color:var(--accent); margin-right:8px;"></i>Phân bổ trạng thái thiết bị</span>
            </div>
            <div class="chart-container">
                <canvas id="statusChart"></canvas>
            </div>
        </div>
        <div class="chart-card">
            <div class="chart-title">
                <span><i class="fa-solid fa-chart-bar" style="color:var(--success); margin-right:8px;"></i>Tỷ trọng Giá trị còn lại vs Khấu hao</span>
            </div>
            <div class="chart-container">
                <canvas id="valueChart"></canvas>
            </div>
        </div>
    </div>

    <!-- BATCH ACTIONS -->
    <div class="batch-bar" v-if="selectedRows.length > 0">
        <span class="batch-count">Đã chọn <b style="color:var(--accent-light); font-size: 15px; margin: 0 4px;">{{ selectedRows.length }}</b> tài sản</span>
        <div class="batch-actions">
            <button class="btn-secondary btn-sm" @@click="exportSelectedCSV" style="color: #1e293b; border: none;"><i class="fa-solid fa-file-csv" style="color:#10b981;"></i> Xuất CSV đã chọn</button>
            <button class="btn-primary btn-sm" v-if="hasPermission('VongDoiKhauHao', 'EDIT')" @@click="openBatchStatusModal" style="border: none;"><i class="fa-solid fa-rotate"></i> Cập nhật trạng thái</button>
        </div>
    </div>

    <!-- FILTERS -->
    <div class="filter-bar">
        <div class="filter-group">
            <label>Trạng thái vòng đời</label>
            <select class="filter-select" v-model="filterStatus">
                <option value="">Tất cả trạng thái</option>
                <option value="active">Đang sử dụng (Active)</option>
                <option value="suspended">Tạm ngưng (Suspended)</option>
                <option value="disposed">Đã thanh lý (Disposed)</option>
                <option value="replaced">Đã thay mới (Replaced)</option>
            </select>
        </div>
        <div class="filter-group">
            <label>Năm hạn sử dụng</label>
            <select class="filter-select" v-model="filterYear">
                <option value="">Tất cả các năm</option>
                <option v-for="y in availableYears" :key="y" :value="y.toString()">Năm {{ y }}</option>
            </select>
        </div>
        <div style="display:flex; gap: 8px; align-items: center;">
            <button class="btn-filter" @@click="refreshData">
                <i class="fa-solid fa-calculator"></i> Tính KH tự động
            </button>
            <button class="btn-secondary" @@click="exportCSV" style="margin-top: 16px; display:flex; align-items:center; gap:6px; font-size: 12.5px; padding: 7px 14px; border-radius: 7px;">
                <i class="fa-solid fa-file-csv" style="color: #10b981;"></i> Xuất Excel/CSV
            </button>
        </div>
    </div>

    <!-- TABLE -->
    <div class="table-card">
        <div class="table-top">
            <div class="table-title">Danh sách khấu hao tài sản</div>
        </div>
        
        <!-- TOP SCROLLBAR -->
        <div class="top-scrollbar" ref="topScroll" @@scroll="syncTopScroll">
            <div class="top-scrollbar-content" :style="{ width: tableWidth + 'px', height: '1px' }"></div>
        </div>

        <div class="table-responsive" ref="bottomScroll" @@scroll="syncBottomScroll">
            <table ref="mainTable">
                <thead>
                    <tr>
                        <th class="checkbox-cell">
                            <input type="checkbox" class="custom-checkbox" 
                            change="toggleSelectAll" :checked="isAllSelected">
                        </th>
                        <th @@click="sortBy('id')" class="sortable">Mã tài sản <i class="fa-solid fa-sort sort-icon" :class="{active: sortKey==='id'}"></i></th>
                        <th @@click="sortBy('name')" class="sortable">Tên thiết bị <i class="fa-solid fa-sort sort-icon" :class="{active: sortKey==='name'}"></i></th>
                        <th>Ngày nhập</th>
                        <th>Hạn BH</th>
                        <th>Hạn sử dụng</th>
                        <th @@click="sortBy('status')" class="sortable">Trạng thái <i class="fa-solid fa-sort sort-icon" :class="{active: sortKey==='status'}"></i></th>
                        <th @@click="sortBy('openingValue')" class="th-money sortable">Nguyên giá (VNĐ) <i class="fa-solid fa-sort sort-icon" :class="{active: sortKey==='openingValue'}"></i></th>
                        <th @@click="sortBy('depreciation')" class="th-money sortable">Khấu hao lũy kế <i class="fa-solid fa-sort sort-icon" :class="{active: sortKey==='depreciation'}"></i></th>
                        <th @@click="sortBy('closingValue')" class="th-money sortable">Giá trị còn lại <i class="fa-solid fa-sort sort-icon" :class="{active: sortKey==='closingValue'}"></i></th>
                        <th>Thay thế bởi</th>
                        <th style="text-align:right">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    <tr v-for="d in paginatedDevices" :key="d.id" :style="selectedRows.includes(d.id) ? 'background:#f8fafc;' : ''">
                        <td class="checkbox-cell">
                            <input type="checkbox" class="custom-checkbox" v-model="selectedRows" :value="d.id">
                        </td>
                        <td><span class="device-id">{{ d.id }}</span></td>
                        <td><div class="device-name">{{ d.name }}</div></td>
                        <td>{{ d.importDate }}</td>
                        <td>{{ d.warrantyExpiry }}</td>
                        <td>{{ d.expiryDate }}</td>
                        <td>
                            <span :class="['status-badge', 's-' + d.status]">
                                {{ statusText(d.status) }}
                            </span>
                        </td>
                        <td class="money">{{ formatMoney(d.openingValue) }}</td>
                        <td style="min-width: 140px;">
                            <div style="display:flex; justify-content:space-between; align-items:flex-end; margin-bottom:4px; font-size:11px;">
                                <span style="color:var(--danger); font-weight:600; font-size: 12.5px; font-family: monospace;">{{ formatMoney(d.depreciation) }}</span>
                                <span style="color:var(--text-muted); font-weight: 600;">{{ calcDepPercent(d) }}%</span>
                            </div>
                            <div style="width:100%; background:#f1f5f9; border-radius:4px; height:6px; overflow:hidden;">
                                <div :style="{width: calcDepPercent(d) + '%', background: 'var(--danger)', height: '100%', transition: 'width 0.5s ease'}"></div>
                            </div>
                        </td>
                        <td class="money" style="color:var(--success)">{{ formatMoney(d.closingValue) }}</td>
                        <td>
                            <span v-if="d.replacedBy" class="replaced-link" title="Xem thiết bị thay thế">{{ d.replacedBy }}</span>
                            <span v-else style="color:var(--muted)">-</span>
                        </td>
                        <td style="text-align:right">
                            <div class="dropdown" :class="{show: activeDropdown === d.id}">
                                <button class="action-btn" @@click.stop="toggleDropdown(d.id)">
                                    <i class="fa-solid fa-ellipsis-vertical"></i>
                                </button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" v-if="hasPermission('VongDoiKhauHao', 'EDIT')" @@click="openStatusModal(d)">
                                        <i class="fa-solid fa-rotate"></i> Cập nhật trạng thái
                                    </a>
                                    <a class="dropdown-item" @@click="openLogModal(d)">
                                        <i class="fa-solid fa-file-invoice-dollar"></i> Xem LS Khấu hao
                                    </a>
                                </div>
                            </div>
                        </td>
                    </tr>
                    <tr v-if="paginatedDevices.length === 0">
                        <td colspan="11" style="text-align:center; padding: 30px; color: var(--muted);">
                            <i class="fa-solid fa-inbox" style="font-size: 24px; margin-bottom: 8px; display: block;"></i>
                            Không tìm thấy tài sản nào phù hợp.
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
        
        <!-- PAGINATION -->
        <div class="pagination" v-if="processedDevices.length > 0">
            <div class="page-info">
                Hiển thị {{ (currentPage - 1) * pageSize + 1 }} - {{ Math.min(currentPage * pageSize, processedDevices.length) }} trong số {{ processedDevices.length }} tài sản
            </div>
            <div class="page-controls">
                <button class="page-btn" :disabled="currentPage === 1" @@click="currentPage--"><i class="fa-solid fa-chevron-left"></i></button>
                <button class="page-btn" v-for="p in visiblePages" :key="p" :class="{active: currentPage === p}" @@click="currentPage = p">{{ p }}</button>
                <button class="page-btn" :disabled="currentPage === totalPages" @@click="currentPage++"><i class="fa-solid fa-chevron-right"></i></button>
            </div>
        </div>
    </div>

    <!-- MODAL: Cập nhật trạng thái -->
    <div class="custom-modal-overlay" v-if="showStatusModal" @@click.self="showStatusModal=false">
        <div class="custom-modal">
            <div class="modal-header">
                <div class="modal-title">Cập nhật vòng đời thiết bị</div>
                <button class="btn-close-modal" @@click="showStatusModal=false"><i class="fa-solid fa-xmark"></i></button>
            </div>
            <div class="modal-body">
                <div class="mb-3">
                    <label class="form-label">Thiết bị đang chọn</label>
                    <div class="form-control" style="background:#f8fafc;font-weight:600;color:var(--accent)">
                        <template v-if="selectedDevice">
                            {{ selectedDevice.id }} - {{ selectedDevice.name }}
                        </template>
                        <template v-else>
                            Nhiều thiết bị (Đang chọn {{ selectedRows.length }} thiết bị)
                        </template>
                    </div>
                </div>
                <div class="mb-3">
                    <label class="form-label">Trạng thái mới (LifeStatus)</label>
                    <select class="form-control" v-model="formStatus">
                        <option value="active">Active - Hoạt động bình thường</option>
                        <option value="suspended">Suspended - Tạm ngưng do hỏng</option>
                        <option value="disposed">Disposed - Thanh lý</option>
                        <option value="replaced">Replaced - Thay thế bằng TB mới</option>
                    </select>
                </div>

                <div class="mb-3" v-if="formStatus === 'replaced'">
                    <label class="form-label">Mã thiết bị thay thế (ReplacedByInventoryId)</label>
                    <input type="text" class="form-control" placeholder="Nhập ID thiết bị mới..." v-model="formReplacedBy">
                    <small style="color:var(--text-muted);font-size:11px;display:block;margin-top:4px;">
                        Liên kết ID này để duy trì lịch sử luân chuyển thiết bị của khoa phòng.
                    </small>
                </div>

                <div class="mb-3" v-if="formStatus === 'disposed' || formStatus === 'suspended'">
                    <label class="form-label">Lý do / Ghi chú</label>
                    <textarea class="form-control" rows="3" placeholder="Nhập lý do..." v-model="formReason"></textarea>
                </div>
            </div>
            <div class="modal-footer">
                <button class="btn-secondary" @@click="showStatusModal=false">Hủy bỏ</button>
                <button class="btn-primary" @@click="saveStatus">Lưu thay đổi</button>
            </div>
        </div>
    </div>

    <!-- MODAL: Lịch sử khấu hao (DepreciationLogs) -->
    <div class="custom-modal-overlay" v-if="showLogModal" @@click.self="showLogModal=false">
        <div class="custom-modal large">
            <div class="modal-header">
                <div class="modal-title">Bảng tính Khấu hao tài sản (DepreciationLogs)</div>
                <button class="btn-close-modal" @@click="showLogModal=false"><i class="fa-solid fa-xmark"></i></button>
            </div>
            <div class="modal-body" style="background:#f1f5f9;">
                <div style="margin-bottom:16px;background:white;padding:14px;border-radius:10px;border:1px solid var(--border);">
                    <div style="font-size:13px;color:var(--muted);font-weight:600;margin-bottom:4px;">THIẾT BỊ</div>
                    <div style="font-weight:700;font-size:16px;color:var(--accent);">{{ selectedDevice?.id }}</div>
                    <div style="font-size:14px;">{{ selectedDevice?.name }}</div>
                </div>

                <div class="log-timeline">
                    <div class="log-item" v-for="log in mockLogs" :key="log.year">
                        <div class="log-header">
                            <span>Khấu hao năm <span class="log-year">{{ log.year }}</span></span>
                            <span class="log-calc">Tính lúc: {{ log.calculatedAt }} | Bởi: Admin</span>
                        </div>
                        <div class="log-stats">
                            <div class="l-stat">
                                <span class="l-label">Giá trị đầu kỳ (Opening)</span>
                                <span class="l-val">{{ formatMoney(log.opening) }}</span>
                            </div>
                            <div class="l-stat">
                                <span class="l-label">Mức khấu hao (Amt)</span>
                                <span class="l-val val-minus">- {{ formatMoney(log.amt) }}</span>
                            </div>
                            <div class="l-stat">
                                <span class="l-label">Giá trị cuối kỳ (Closing)</span>
                                <span class="l-val" style="color:var(--success)">{{ formatMoney(log.closing) }}</span>
                            </div>
                        </div>
                        <div class="log-note">
                            <strong>Ghi chú:</strong> {{ log.note }}
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer" style="background:white">
                <button class="btn-secondary" @@click="showLogModal=false">Đóng</button>
            </div>
        </div>
    </div>

    <!-- MODAL: Thêm tài sản mới -->
    <div class="custom-modal-overlay" v-if="showAddAssetModal" @@click.self="showAddAssetModal=false">
        <div class="custom-modal large">
            <div class="modal-header">
                <div class="modal-title"><i class="fa-solid fa-circle-plus" style="color:var(--accent); margin-right: 6px;"></i>Thêm tài sản mới</div>
                <button class="btn-close-modal" @@click="showAddAssetModal=false"><i class="fa-solid fa-xmark"></i></button>
            </div>
            <div class="modal-body" style="background:#f8fafc; padding: 20px;">
                <div style="display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px;">
                    <!-- Mã tài sản -->
                    <div>
                        <label class="form-label">Mã tài sản <span style="color:var(--danger)">*</span></label>
                        <input type="text" class="form-control" placeholder="Ví dụ: PC-2026-001" v-model="formAddAsset.assetCode" required>
                    </div>
                    <!-- Tên thiết bị -->
                    <div>
                        <label class="form-label">Tên thiết bị <span style="color:var(--danger)">*</span></label>
                        <input type="text" class="form-control" placeholder="Nhập tên thiết bị..." v-model="formAddAsset.itemName" required>
                    </div>
                    <!-- Nhóm thiết bị -->
                    <div>
                        <label class="form-label">Nhóm thiết bị <span style="color:var(--danger)">*</span></label>
                        <select class="form-control" v-model="formAddAsset.groupId" required>
                            <option value="">-- Chọn nhóm thiết bị --</option>
                            <option v-for="grp in groupsList" :key="grp.Id" :value="grp.Id">{{ grp.Name }}</option>
                        </select>
                    </div>
                    <!-- Số Serial -->
                    <div>
                        <label class="form-label">Số Serial</label>
                        <input type="text" class="form-control" placeholder="Nhập số Serial..." v-model="formAddAsset.serialNumber">
                    </div>
                    <!-- Số lượng -->
                    <div>
                        <label class="form-label">Số lượng <span style="color:var(--danger)">*</span></label>
                        <input type="number" class="form-control" v-model.number="formAddAsset.quantity" min="1" required>
                    </div>
                    <!-- Khoa / Phòng ban -->
                    <div>
                        <label class="form-label">Khoa / Phòng ban sử dụng <span style="color:var(--danger)">*</span></label>
                        <select class="form-control" v-model="formAddAsset.departmentId" required>
                            <option value="">-- Chọn khoa phòng --</option>
                            <option v-for="dept in departmentsList" :key="dept.Id" :value="dept.Id">{{ dept.Name }}</option>
                        </select>
                    </div>
                    <!-- Vị trí lắp đặt -->
                    <div>
                        <label class="form-label">Vị trí cụ thể <span style="color:var(--danger)">*</span></label>
                        <select class="form-control" v-model="formAddAsset.locationId" required>
                            <option value="">-- Chọn vị trí --</option>
                            <option v-for="loc in locationsList" :key="loc.Id" :value="loc.Id">{{ loc.Name }}</option>
                        </select>
                    </div>
                    <!-- Ngày nhập -->
                    <div>
                        <label class="form-label">Ngày nhập kho <span style="color:var(--danger)">*</span></label>
                        <input type="date" class="form-control" v-model="formAddAsset.importDate" required>
                    </div>
                    <!-- Đơn giá -->
                    <div>
                        <label class="form-label">Đơn giá (VNĐ) <span style="color:var(--danger)">*</span></label>
                        <input type="number" class="form-control" placeholder="Nhập đơn giá..." v-model.number="formAddAsset.unitPrice" required>
                    </div>
                    <!-- Tỷ lệ khấu hao (%) -->
                    <div>
                        <label class="form-label">Tỷ lệ khấu hao (%)</label>
                        <input type="number" class="form-control" placeholder="Ví dụ: 25" v-model.number="formAddAsset.depreciationRate" min="0" max="100">
                    </div>
                    <!-- Thời gian khấu hao (Năm) -->
                    <div>
                        <label class="form-label">TG Khấu hao (Năm)</label>
                        <input type="number" class="form-control" placeholder="Ví dụ: 4" v-model.number="formAddAsset.depreciationYears" min="1">
                    </div>
                    <!-- Hạn bảo hành -->
                    <div>
                        <label class="form-label">Hạn bảo hành</label>
                        <input type="date" class="form-control" v-model="formAddAsset.warrantyExpiry">
                    </div>
                    <!-- Hạn sử dụng -->
                    <div>
                        <label class="form-label">Hạn sử dụng</label>
                        <input type="date" class="form-control" v-model="formAddAsset.expiryDate">
                    </div>
                </div>
                <!-- Thành tiền -->
                <div style="margin-top: 16px; background:#eff6ff; padding: 12px; border-radius: 8px; border: 1px solid #bfdbfe; display: flex; justify-content: space-between; align-items: center;">
                    <span style="font-weight:600; color:#1e40af;">TỔNG GIÁ TRỊ (Thành tiền):</span>
                    <span style="font-size:18px; font-weight:700; color:var(--accent);">{{ formatMoney(formAddAsset.unitPrice * formAddAsset.quantity || 0) }} đ</span>
                </div>
                <!-- Ghi chú -->
                <div style="margin-top: 16px;">
                    <label class="form-label">Mô tả / Ghi chú</label>
                    <textarea class="form-control" rows="2" placeholder="Nhập ghi chú thêm..." v-model="formAddAsset.note"></textarea>
                </div>
            </div>
            <div class="modal-footer">
                <button class="btn-secondary" @@click="showAddAssetModal=false">Hủy bỏ</button>
                <button class="btn-primary" @@click="saveAddAsset" :disabled="isSavingAsset">
                    <span v-if="isSavingAsset"><i class="fa-solid fa-spinner fa-spin"></i> Đang lưu...</span>
                    <span v-else>Lưu tài sản</span>
                </button>
            </div>
        </div>
    </div>
</div>

@section scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        window.DevicesJsonData = @Html.Raw(ViewBag.DevicesJson ?? "[]");
        window.GroupsJsonData = @Html.Raw(ViewBag.GroupsJson ?? "[]");
        window.DepartmentsJsonData = @Html.Raw(ViewBag.DepartmentsJson ?? "[]");
        window.LocationsJsonData = @Html.Raw(ViewBag.LocationsJson ?? "[]");
    </script>
    <script src="~/Scripts/app/VongDoiKhauHao.js?v=@DateTime.Now.Ticks"></script>
}
